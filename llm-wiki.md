# LLM Wiki — Self-Improvement Memory

> Cap: 200 entries. Oldest entries are trimmed first. One entry per line block.

## 2026-08-13

- **A-001** — Gradio 6.x: mount into FastAPI with `app = gr.Blocks(); app.mount(fastapi_app)`. Confidence: HIGH
- **A-002** — edge-tts needs no keys; pass `rate`/`pitch` params for DSP-style voice. Confidence: HIGH
- **A-003** — OpenRouter `:free` models change; always resolve model ids from `https://openrouter.ai/api/v1/models` before use. Confidence: HIGH
- **A-004** — uv: `uv sync` installs `[project.scripts]` entrypoints; run with `uv run cawl`. Confidence: HIGH
